server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.lightblue-cloudant-s7.credentials.username}
    password: ${vcap.services.lightblue-cloudant-s7.credentials.password}
    host: ${vcap.services.lightblue-cloudant-s7.credentials.host}
    protocol: https
    port: 443
    database: customers
    